#!/usr/bin/env bash

# Tell the scripts they're being run from the "launcher".
export BSP_LAUNCHER=1
rofi -show combi -modi combi,window,windowcd,run,drun -combi-modi 'run,drun,window'
# rofi -show combi
